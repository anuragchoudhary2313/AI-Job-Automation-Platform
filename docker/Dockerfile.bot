FROM python:3.11-slim

WORKDIR /app

# Install chrome/driver dependencies if needed for Selenium
# For now, just basic python setup
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "main.py"]
